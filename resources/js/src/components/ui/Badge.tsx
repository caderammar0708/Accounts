

// import React from 'react';
// import { TempRegStatus } from '@/src/types';

// interface BadgeProps {
//   status: TempRegStatus | MedicalStatus | string;
// }

// const getStatusColor = (status: BadgeProps['status']) => {
//   switch (status) {
//     case TempRegStatus.REJECTED:
//       return 'bg-red-100 text-red-800';
//     default:
//       return 'bg-gray-100 text-gray-800';
//   }
// };

// const Badge: React.FC<BadgeProps> = ({ status }) => {
//   return (
//     <span
//       className={`px-2.5 py-0.5 text-xs font-medium leading-5 rounded-full ${getStatusColor(status)}`}
//     >
//       {status}
//     </span>
//   );
// };

// export default Badge;

